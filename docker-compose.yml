version: '3.8'

services:
  app:
    build:
      context: .
      target: production
    ports:
      - "80:3333"
    environment:
      - NODE_ENV=production
    env_file:
      - .env.production
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
